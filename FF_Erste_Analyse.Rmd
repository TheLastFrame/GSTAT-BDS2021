---
title: "Erste Analyse"
author: "Fabian Fiedler"
date: "1/18/2021"
output:
  pdf_document: default
  html_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message=FALSE, comment=NULL)
```


```{r}
library(tidyverse)
library(magrittr)
library(readxl)
library(knitr)
```

```{r}
studdat <- read_excel(file.path("Daten","StudierendenDaten",
                                "StudDat.xlsx"))
```

```{r}
names(studdat)
```

```{r}
(studdat %>% names())[1:3]
```

```{r}
studdat %>%
  select(Gender, Size, Weight) %>%
  drop_na() %>%
  group_by(Gender) %>%
  summarise(mittlere_Größe=mean(Size), mittleres_Gewicht=mean(Weight)) %>%
  kable()
```

```{r}
studdat %>%
  select(Gender, Size, Weight) %>%
  drop_na()%>%
  ggplot(aes(x=Size, y=Weight)) + geom_point()
```


